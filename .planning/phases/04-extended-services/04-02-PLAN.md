---
phase: 04-extended-services
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [lib/data/datasources/radarr_datasource.dart, lib/data/repositories/radarr_repository_impl.dart, lib/domain/repositories/radarr_repository.dart, lib/presentation/blocs/radarr_bloc.dart, lib/presentation/pages/radarr_page.dart, lib/core/di/injection_container.dart]
autonomous: true
requirements: [APP-EXT-02]
must_haves:
  truths:
    - "User can view Radarr movie library and download status"
  artifacts:
    - path: "lib/data/datasources/radarr_datasource.dart"
      provides: "Radarr API client"
    - path: "lib/presentation/pages/radarr_page.dart"
      provides: "Radarr monitoring UI"
  key_links:
    - from: "lib/presentation/pages/radarr_page.dart"
      to: "Radarr API"
      via: "RadarrBloc -> RadarrRepository"
---

<objective>
Implement the Radarr module, including data fetching, business logic, and UI for monitoring movies.

Purpose: Provide visibility into the movie library and download queue.
Output: Working Radarr integration with a dedicated page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-extended-services/04-RESEARCH.md
@lib/data/datasources/lidarr_datasource.dart
@lib/presentation/pages/lidarr_page.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Radarr Data Layer</name>
  <files>lib/data/datasources/radarr_datasource.dart, lib/domain/repositories/radarr_repository.dart, lib/data/repositories/radarr_repository_impl.dart</files>
  <action>
    Create RadarrDataSource focusing on GET /api/v3/movie and GET /api/v3/queue.
    Use LidarrDataSource as a pattern for API Key retrieval from SecureStorage.
    Implement RadarrRepository to bridge the datasource and domain.
  </action>
  <verify>
    <automated># No tests yet, verify file existence and syntax</automated>
  </verify>
  <done>Data layer for Radarr implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Radarr BLoC</name>
  <files>lib/presentation/blocs/radarr_bloc.dart</files>
  <action>
    Create RadarrBloc using the patterns from LidarrBloc.
    Events: FetchMovies, RefreshQueue.
    States: Loading, Loaded, Error.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>RadarrBloc implemented and states generated.</done>
</task>

<task type="auto">
  <name>Task 3: Create Radarr Page and Register DI</name>
  <files>lib/presentation/pages/radarr_page.dart, lib/core/di/injection_container.dart</files>
  <action>
    Create RadarrPage to display the movie library and download queue.
    Register RadarrDataSource, RadarrRepository, and RadarrBloc in injection_container.dart.
  </action>
  <verify>
    <automated># Manual verification in next wave</automated>
  </verify>
  <done>Radarr Page created and dependency injection configured.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | RadarrDataSource | mitigate | API Keys are used only in headers, never logged. |
</threat_model>

<success_criteria>
Radarr module is fully implemented and ready for integration.
</success_criteria>

<output>
After completion, create .planning/phases/04-extended-services/04-02-SUMMARY.md
</output>
