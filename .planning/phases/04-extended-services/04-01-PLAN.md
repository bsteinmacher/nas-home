---
phase: 04-extended-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/data/repositories/registry_repository_impl.dart, lib/domain/entities/radarr.dart, lib/domain/entities/sonarr.dart, lib/domain/entities/prowlarr.dart]
autonomous: true
requirements: [APP-EXT-01, APP-EXT-02, APP-EXT-03, APP-EXT-04]
must_haves:
  truths:
    - "Registry sync handles keys with both 'service' and 'service_key' formats"
    - "Radarr, Sonarr, and Prowlarr entities exist and are serializable via Freezed"
  artifacts:
    - path: "lib/data/repositories/registry_repository_impl.dart"
      provides: "Fix for registry key mismatch"
    - path: "lib/domain/entities/radarr.dart"
      provides: "RadarrMovie entity"
    - path: "lib/domain/entities/sonarr.dart"
      provides: "SonarrSeries entity"
    - path: "lib/domain/entities/prowlarr.dart"
      provides: "ProwlarrIndexer entity"
  key_links:
    - from: "lib/data/repositories/registry_repository_impl.dart"
      to: "nas-registry backend"
      via: "key name matching"
---

<objective>
Fix the registry synchronization bug and define the data structures (entities) for Radarr, Sonarr, and Prowlarr.

Purpose: Ensure the app correctly receives API keys from the backend and has a solid foundation for the new services.
Output: Fixed sync logic and new entity files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-extended-services/04-RESEARCH.md
@lib/data/repositories/registry_repository_impl.dart
@services/registry/app/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RegistryRepositoryImpl key mapping</name>
  <files>lib/data/repositories/registry_repository_impl.dart</files>
  <action>
    Modify the syncConfig method to check for both 'service' and 'service_key' (e.g., 'radarr' and 'radarr_key') to match the backend schema (D-04-01).
    Update mappings for:
    - radarr / radarr_key
    - sonarr / sonarr_key
    - prowlarr / prowlarr_key
    - lidarr / lidarr_key
    - seerr / seerr_key
  </action>
  <verify>
    <automated>flutter test test/data/repositories/registry_repository_test.dart</automated>
  </verify>
  <done>RegistryRepositoryImpl correctly saves keys regardless of the '_key' suffix.</done>
</task>

<task type="auto">
  <name>Task 2: Define Entities for Arr Services</name>
  <files>lib/domain/entities/radarr.dart, lib/domain/entities/sonarr.dart, lib/domain/entities/prowlarr.dart</files>
  <action>
    Create Freezed entities for:
    - RadarrMovie (id, title, year, status, monitored, remotePoster, sizeOnDisk)
    - SonarrSeries (id, title, year, status, monitored, remotePoster, seasonCount)
    - ProwlarrIndexer (id, name, protocol, status, enabled)
    
    Ensure they have fromJson/toJson methods. Run 'flutter pub run build_runner build' after creation.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Entity files created and G.dart/FREEZED.dart files generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> Repo | Registry data (keys) entering the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | RegistryRepositoryImpl | mitigate | Keys are stored in SecureStorage immediately upon receipt. |
</threat_model>

<verification>
Check that build_runner completes without errors and the new entities are usable.
</verification>

<success_criteria>
Registry sync works with the backend (tested via unit tests) and new models are ready for use.
</success_criteria>

<output>
After completion, create .planning/phases/04-extended-services/04-01-SUMMARY.md
</output>
