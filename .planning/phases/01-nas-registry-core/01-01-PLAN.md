---
phase: 01-nas-registry-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "services/registry/pyproject.toml",
  "services/registry/.gitignore",
  "services/registry/.env",
  "services/registry/.env.example",
  "services/registry/app/main.py",
  "services/registry/app/security.py",
  "services/registry/app/schemas.py",
  "services/registry/app/__init__.py",
  "services/registry/tests/__init__.py",
  "services/registry/tests/test_health.py",
  "services/registry/tests/test_auth.py",
  "services/registry/README.md"
]
autonomous: true
requirements: [NAS-REG-01, NAS-REG-05]

must_haves:
  truths:
    - "Service starts and listens on port 8000"
    - "Accessing protected routes without X-Registry-Token returns 403"
    - "Accessing with valid X-Registry-Token returns 200"
  artifacts:
    - path: "services/registry/app/main.py"
      provides: "FastAPI entry point"
    - path: "services/registry/app/security.py"
      provides: "X-Registry-Token validation logic"
    - path: "services/registry/pyproject.toml"
      provides: "Project dependencies including pytest and uvicorn"
    - path: "services/registry/.gitignore"
      provides: "Exclusion of .env and __pycache__"
  key_links:
    - from: "services/registry/app/main.py"
      to: "services/registry/app/security.py"
      via: "Depends(validate_token)"
---

<objective>
Setup the foundational Python environment for the NAS Registry microservice, implement the core FastAPI application skeleton, and enforce the X-Registry-Token security requirement.

Purpose: Ensure a secure and standard base for the discovery service.
Output: A running FastAPI service with token-based authentication and a configured test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-nas-registry-core/01-RESEARCH.md
@.planning/phases/01-nas-registry-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Project & Dependencies</name>
  <files>services/registry/pyproject.toml, services/registry/.gitignore, services/registry/.env, services/registry/.env.example, services/registry/README.md</files>
  <action>
    Create the project structure and manage dependencies using pyproject.toml. 
    Include: fastapi, uvicorn, python-dotenv, pydantic-settings. 
    Include dev dependencies: pytest, httpx.
    
    1. Create pyproject.toml with the specified dependencies.
    2. Install dependencies: pip install "fastapi[standard]" uvicorn pytest httpx python-dotenv pydantic-settings
    3. Create a .gitignore for services/registry/ that excludes .env, __pycache__, .pytest_cache, and .venv.
    4. Create a .env.example with placeholders for REGISTRY_TOKEN and paths to service configs (LIDARR_CONFIG_PATH, etc.).
    5. Create a .env file (copy of .env.example) and set a default REGISTRY_TOKEN=dev-token-123.
    6. Implement a basic README.md with "How to Run" instructions.
  </action>
  <verify>
    <automated>ls services/registry/pyproject.toml services/registry/.gitignore services/registry/.env services/registry/.env.example && pip show fastapi</automated>
  </verify>
  <done>Project configuration files exist, dependencies are installed, and .env is initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FastAPI Skeleton & Health Check</name>
  <files>services/registry/app/main.py, services/registry/app/schemas.py, services/registry/app/__init__.py, services/registry/tests/__init__.py, services/registry/tests/test_health.py</files>
  <action>
    Create app/__init__.py and tests/__init__.py to establish packages.
    Create app/main.py with a basic FastAPI instance and a health check route GET /.
    Define Pydantic models in app/schemas.py for the RegistryConfig response, including optional fields for lidarr_key, radarr_key, sonarr_key, prowlarr_key, and seerr_key.
    Create tests/test_health.py using fastapi.testclient.TestClient to verify the GET / endpoint returns {"status": "ok"}.
  </action>
  <verify>
    <automated>cd services/registry && python3 -m pytest tests/test_health.py</automated>
  </verify>
  <done>FastAPI app is initialized with a health check route verified by an automated test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement X-Registry-Token Security</name>
  <files>services/registry/app/security.py, services/registry/app/main.py, services/registry/tests/test_auth.py</files>
  <behavior>
    - GET /config without X-Registry-Token -> 403 Forbidden
    - GET /config with invalid X-Registry-Token -> 403 Forbidden
    - GET /config with valid X-Registry-Token (matching REGISTRY_TOKEN env) -> 200 OK
  </behavior>
  <action>
    Implement validate_token using fastapi.security.APIKeyHeader in app/security.py.
    Use secrets.compare_digest for constant-time comparison per RESEARCH.md.
    Integrate this dependency into a stubbed GET /config route in app/main.py.
    Create tests/test_auth.py using fastapi.testclient.TestClient to verify these behaviors.
  </action>
  <verify>
    <automated>export REGISTRY_TOKEN=test-token && cd services/registry && python3 -m pytest tests/test_auth.py</automated>
  </verify>
  <done>Security layer correctly intercepts and validates the custom header, verified by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted requests from local network/VPN cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | GET /config | mitigate | Enforce X-Registry-Token header validation for all protected endpoints. |
| T-01-02 | Information Disclosure | Config Keys | mitigate | Use secrets.compare_digest to prevent timing attacks on the token. |
| T-01-03 | Tampering | Environment Variables | mitigate | Store sensitive tokens in .env, not in source code. |
</threat_model>

<verification>
- Run all tests: `cd services/registry && python3 -m pytest`.
- Verify 403 for unauthorized requests to /config.
- Verify 200 for authorized requests with the correct token via pytest.
</verification>

<success_criteria>
- FastAPI service project structure initialized.
- Dependencies installed.
- / health check returns 200 OK.
- Protected route rejects requests without valid token.
- Protected route accepts requests with valid token.
- Pytest suite for auth passes.
</success_criteria>

<output>
After completion, create .planning/phases/01-nas-registry-core/01-01-SUMMARY.md
</output>
