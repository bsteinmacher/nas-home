---
phase: 01-nas-registry-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "services/registry/app/scanner.py",
  "services/registry/app/main.py",
  "services/registry/tests/test_scanner.py",
  "services/registry/tests/test_api.py",
  "services/registry/tests/mocks/lidarr_config.xml",
  "services/registry/tests/mocks/seerr_settings.json"
]
autonomous: true
requirements: [NAS-REG-02, NAS-REG-03, NAS-REG-04]

must_haves:
  truths:
    - "GET /config returns JSON with keys extracted from mock files"
    - "XML scanner correctly extracts <ApiKey> from Lidarr-style config"
    - "JSON scanner correctly extracts 'apiKey' from Seerr-style settings"
  artifacts:
    - path: "services/registry/app/scanner.py"
      provides: "Config parsing logic for XML and JSON"
    - path: "services/registry/tests/test_scanner.py"
      provides: "Unit tests for different config formats"
    - path: "services/registry/tests/mocks/lidarr_config.xml"
      provides: "Mock Lidarr configuration for testing"
    - path: "services/registry/tests/mocks/seerr_settings.json"
      provides: "Mock Seerr configuration for testing"
  key_links:
    - from: "services/registry/app/main.py"
      to: "services/registry/app/scanner.py"
      via: "Function calls in /config handler"
---

<objective>
Implement the core logic for discovering API keys by parsing service configuration files (XML for *Arr services and JSON for Seerr/Jellyseerr) and expose this data via the secure /config endpoint.

Purpose: Automate the discovery of NAS service credentials.
Output: A functional API endpoint that returns real or mocked service keys.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-nas-registry-core/01-RESEARCH.md
@.planning/phases/01-nas-registry-core/01-VALIDATION.md
@services/registry/app/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement XML/JSON Scanner Logic & Mocks</name>
  <files>services/registry/app/scanner.py, services/registry/tests/test_scanner.py, services/registry/tests/mocks/lidarr_config.xml, services/registry/tests/mocks/seerr_settings.json</files>
  <behavior>
    - XML Scanner: Given a Lidarr config.xml, findtext('ApiKey') -> "LIDARR_KEY"
    - JSON Scanner: Given a Seerr settings.json, get('apiKey') -> "SEERR_KEY"
    - Error Handling: If file missing or tag missing, return None (graceful failure)
  </behavior>
  <action>
    Create tests/mocks/lidarr_config.xml and tests/mocks/seerr_settings.json with valid test data.
    Implement get_servarr_key(file_path) using xml.etree.ElementTree as per RESEARCH.md.
    Implement get_seerr_key(file_path) using the standard json library.
    Create unit tests in tests/test_scanner.py to verify extraction logic using the created mock files.
  </action>
  <verify>
    <automated>cd services/registry && python3 -m pytest tests/test_scanner.py</automated>
  </verify>
  <done>Scanner logic accurately extracts keys from various file formats and is verified by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Scanners to /config Endpoint & Integration Test</name>
  <files>services/registry/app/main.py, services/registry/tests/test_api.py</files>
  <action>
    Update the GET /config route in app/main.py to use the scanner functions.
    Read file paths from environment variables (LIDARR_CONFIG_PATH, etc.) using Pydantic Settings.
    Aggregate findings into the RegistryConfig response schema.
    Create tests/test_api.py to verify the full flow: authenticating with X-Registry-Token and receiving the discovered keys from the mock files.
  </action>
  <verify>
    <automated>export REGISTRY_TOKEN=test-token && export LIDARR_CONFIG_PATH=tests/mocks/lidarr_config.xml && cd services/registry && python3 -m pytest tests/test_api.py</automated>
  </verify>
  <done>API returns discovered keys in a structured JSON format when called with a valid token, verified by integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Filesystem | The service reads configuration files from the host/volume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Config Files | mitigate | Limit the service to read-only access to specific config files. |
| T-01-05 | Denial of Service | Scanner | mitigate | Use standard library parsers (ElementTree) which are resistant to basic XML bomb attacks for small files. |
</threat_model>

<verification>
- Verify that keys are correctly extracted from mock files via the API.
- Verify that missing files do not crash the service (return null for those keys).
- Run all tests: `cd services/registry && python3 -m pytest`.
</verification>

<success_criteria>
- GET /config returns correct keys for Lidarr and Seerr (at minimum).
- Unit tests pass for both XML and JSON parsing.
- Integration tests pass for the secured endpoint.
- Response matches the defined Pydantic schema.
</success_criteria>

<output>
After completion, create .planning/phases/01-nas-registry-core/01-02-SUMMARY.md
</output>
