---
phase: 03-app-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [lib/data/repositories/registry_repository_impl.dart, lib/data/datasources/lidarr_datasource.dart, lib/data/datasources/seerr_datasource.dart, lib/core/di/injection_container.dart]
autonomous: true
requirements: [APP-REG-03]
must_haves:
  truths:
    - "RegistryRepositoryImpl saves fetched API keys to SecureStorage"
    - "LidarrDataSource reads its API key from SecureStorage"
    - "SeerrDataSource reads its API key from SecureStorage"
  artifacts:
    - path: "lib/data/repositories/registry_repository_impl.dart"
      contains: "secureStorage"
    - path: "lib/data/datasources/lidarr_datasource.dart"
      contains: "secureStorage"
    - path: "lib/data/datasources/seerr_datasource.dart"
      contains: "secureStorage"
  key_links:
    - from: "lib/data/repositories/registry_repository_impl.dart"
      to: "lib/core/storage/secure_storage_service.dart"
      via: "Method injection/Constructor"
    - from: "lib/data/datasources/lidarr_datasource.dart"
      to: "lib/core/storage/secure_storage_service.dart"
      via: "Constructor injection"
---

<objective>
Update the Registry Repository and service DataSources to use the newly implemented `SecureStorageService`. This migrates sensitive API key handling from plain-text `SharedPreferences` to secure local storage.

Purpose: Implement secure persistence and retrieval of service credentials.
Output: Updated Repository and DataSources with secure storage integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-app-integration/03-01-SUMMARY.md
@lib/data/repositories/registry_repository_impl.dart
@lib/data/datasources/lidarr_datasource.dart
@lib/data/datasources/seerr_datasource.dart
@lib/core/di/injection_container.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RegistryRepositoryImpl for secure storage</name>
  <files>lib/data/repositories/registry_repository_impl.dart</files>
  <action>
    1. Update constructor to accept `SecureStorageService`.
    2. In `syncConfig`, replace calls to `sharedPreferences.setString` for sensitive keys (seerr, lidarr, prowlarr, radarr, sonarr, registry_token) with `secureStorage.write`.
    3. Keep `nas_url` in `sharedPreferences` as it's non-sensitive.
  </action>
  <verify>
    <automated>grep "secureStorage.write" lib/data/repositories/registry_repository_impl.dart</automated>
  </verify>
  <done>RegistryRepositoryImpl uses secure storage for API keys.</done>
</task>

<task type="auto">
  <name>Task 2: Update DataSources for secure storage</name>
  <files>lib/data/datasources/lidarr_datasource.dart, lib/data/datasources/seerr_datasource.dart</files>
  <action>
    For both `LidarrDataSourceImpl` and `SeerrDataSourceImpl`:
    1. Update constructor to accept `SecureStorageService`.
    2. Change the `_apiKey` getter (or relevant logic) to be asynchronous or fetch the key from `secureStorage.read`.
    3. Update API call methods to await the key before making requests.
    
    Note: Since `_apiKey` was a synchronous getter using `sharedPreferences`, you'll need to update callsites to handle the Future or use a late initialization pattern if safe.
  </action>
  <verify>
    <automated>grep "secureStorage.read" lib/data/datasources/lidarr_datasource.dart && grep "secureStorage.read" lib/data/datasources/seerr_datasource.dart</automated>
  </verify>
  <done>DataSources retrieve keys securely.</done>
</task>

<task type="auto">
  <name>Task 3: Update DI registrations</name>
  <files>lib/core/di/injection_container.dart</files>
  <action>
    Update the registrations for `RegistryRepository`, `LidarrDataSource`, and `SeerrDataSource` to include `sl<SecureStorageService>()` in their constructors.
  </action>
  <verify>
    <automated>grep "sl<SecureStorageService>()" lib/core/di/injection_container.dart</automated>
  </verify>
  <done>Dependency injection updated for repositories and datasources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → SecureStorage | Storage of decrypted keys |
| DataSource → SecureStorage | Retrieval of keys for API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Memory | accept | Keys reside in memory briefly during API calls; standard Flutter behavior. |
</threat_model>

<verification>
- Verify that `RegistryRepositoryImpl` compiles.
- Verify that `LidarrDataSourceImpl` and `SeerrDataSourceImpl` compile and handle the async nature of secure storage.
</verification>

<success_criteria>
- API keys are no longer written to `SharedPreferences`.
- DataSources correctly pull keys from `SecureStorageService`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-integration/03-02-SUMMARY.md`
</output>
