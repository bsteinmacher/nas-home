---
phase: 03-app-integration
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [lib/presentation/pages/settings_page.dart]
autonomous: false
requirements: [APP-REG-04]
must_haves:
  truths:
    - "User can trigger sync from SettingsPage"
    - "SettingsPage displays visual feedback during and after sync"
    - "Fetched keys are applied and usable by the app"
  artifacts:
    - path: "lib/presentation/pages/settings_page.dart"
      contains: "sl<SecureStorageService>()"
  key_links:
    - from: "lib/presentation/pages/settings_page.dart"
      to: "lib/domain/usecases/sync_registry_config.dart"
      via: "User action (Button Click)"
---

<objective>
Finalize the sync integration in the `SettingsPage` UI. This includes updating the page to use `SecureStorageService` for reading/writing initial config and providing a clear, state-aware "Sync" experience for the user.

Purpose: Provide a seamless and responsive UI for the discovery sync process.
Output: Refined SettingsPage and verified end-to-end sync flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-app-integration/03-02-SUMMARY.md
@lib/presentation/pages/settings_page.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SettingsPage to use SecureStorageService</name>
  <files>lib/presentation/pages/settings_page.dart</files>
  <action>
    1. Import `lib/core/storage/secure_storage_service.dart`.
    2. Add `final _secureStorage = sl<SecureStorageService>();` to `_SettingsPageState`.
    3. Update `initState` to read the `registry_token` and service keys from `_secureStorage` instead of `_prefs`.
    4. Update `_syncFromRegistry` to handle UI state updates after sync (reading the new keys from secure storage).
    5. Update `_saveSettings` to write the `registry_token` to `_secureStorage` if modified manually.
  </action>
  <verify>
    <automated>grep "_secureStorage.read" lib/presentation/pages/settings_page.dart</automated>
  </verify>
  <done>SettingsPage integrated with secure storage.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Sync Status UX</name>
  <files>lib/presentation/pages/settings_page.dart</files>
  <action>
    Ensure that the `_isSyncing` state is properly used to disable buttons and show progress indicators.
    Add a clearer success/error message handling (already partially present, ensure it works with the new flow).
    Optionally: Add a "Last Synced" timestamp or similar indicator to show the user when discovery was last run.
  </action>
  <verify>
    <automated>grep "_isSyncing" lib/presentation/pages/settings_page.dart</automated>
  </verify>
  <done>Sync UX refined.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Registry Discovery Sync flow with Secure Storage.</what-built>
  <how-to-verify>
    1. Open Settings in the app.
    2. Enter NAS URL and Registry Token.
    3. Click "RUN AUTO DISCOVERY SYNC".
    4. Verify success message appears.
    5. Navigate to Lidarr or Seerr page.
    6. Verify that the app can now fetch data (showing that the API key was correctly saved and retrieved).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Repository | Initiation of sync process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | UI | mitigate | Obscure the Registry Token input field in SettingsPage (obscureText: true) |
</threat_model>

<verification>
- Verify that the app builds and runs.
- Verify that the Registry token is obscured in the UI.
</verification>

<success_criteria>
- User can successfully sync keys from the NAS Registry.
- App functionality (Lidarr/Seerr) works correctly after sync.
- Keys are NOT visible in plain-text backups or standard filesystem explorers on rooted devices (as verified by using secure storage).
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-integration/03-03-SUMMARY.md`
</output>
