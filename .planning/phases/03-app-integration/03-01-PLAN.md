---
phase: 03-app-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/core/storage/secure_storage_service.dart, lib/core/di/injection_container.dart]
autonomous: true
requirements: [APP-REG-01, APP-REG-02]
must_haves:
  truths:
    - "Secure storage dependency is available in the project"
    - "SecureStorageService abstraction exists and is injectable"
    - "System can write and read from secure storage"
  artifacts:
    - path: "lib/core/storage/secure_storage_service.dart"
      provides: "Encryption-at-rest for sensitive tokens"
    - path: "pubspec.yaml"
      contains: "flutter_secure_storage"
  key_links:
    - from: "lib/core/di/injection_container.dart"
      to: "lib/core/storage/secure_storage_service.dart"
      via: "Dependency Injection"
---

<objective>
Setup the foundational secure storage infrastructure using `flutter_secure_storage`. This ensures that sensitive API keys and the Registry Token are encrypted at rest on the device, addressing security concerns identified in RESEARCH.md.

Purpose: Move away from plain-text storage in SharedPreferences for sensitive data.
Output: SecureStorageService implementation and updated dependency graph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-app-integration/03-RESEARCH.md
@pubspec.yaml
@lib/core/di/injection_container.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flutter_secure_storage dependency</name>
  <files>pubspec.yaml</files>
  <action>
    Add `flutter_secure_storage: ^9.2.2` to the dependencies section of `pubspec.yaml`.
    Run `flutter pub get` to install the package.
  </action>
  <verify>
    <automated>grep "flutter_secure_storage" pubspec.yaml && flutter pub get</automated>
  </verify>
  <done>Dependency added and installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SecureStorageService</name>
  <files>lib/core/storage/secure_storage_service.dart</files>
  <action>
    Create `lib/core/storage/secure_storage_service.dart` with an interface and its implementation using `FlutterSecureStorage`.
    
    Methods to include:
    - `Future<void> write(String key, String value)`
    - `Future<String?> read(String key)`
    - `Future<void> delete(String key)`
    - `Future<bool> containsKey(String key)`
    
    Implementation should use `const FlutterSecureStorage(aOptions: AndroidOptions(encryptedSharedPreferences: true))`.
  </action>
  <verify>
    <automated>test -f lib/core/storage/secure_storage_service.dart && grep "FlutterSecureStorage" lib/core/storage/secure_storage_service.dart</automated>
  </verify>
  <done>SecureStorageService implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Register SecureStorageService in DI</name>
  <files>lib/core/di/injection_container.dart</files>
  <action>
    1. Import `lib/core/storage/secure_storage_service.dart`.
    2. Register `SecureStorageService` as a LazySingleton in the `init` function of `injection_container.dart`.
  </action>
  <verify>
    <automated>grep "SecureStorageService" lib/core/di/injection_container.dart</automated>
  </verify>
  <done>SecureStorageService registered in GetIt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → OS Storage | Sensitive data crossing into persistent storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Storage | mitigate | Use flutter_secure_storage (Keychain/Keystore) instead of shared_preferences |
| T-03-02 | Tampering | API Keys | mitigate | Use encryptedSharedPreferences: true on Android to prevent file manipulation |
</threat_model>

<verification>
- Run `flutter pub get` to ensure no dependency conflicts.
- Verify `SecureStorageService` file existence and basic syntax.
</verification>

<success_criteria>
- Project compiles with new dependency.
- `SecureStorageService` is available for injection in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-integration/03-01-SUMMARY.md`
</output>
